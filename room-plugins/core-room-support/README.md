Shared support for the room plugins that were ported out of the core desktop app.

This directory intentionally contains pure helper modules that can be imported by
multiple room plugins in this repo. It is not itself a plugin and does not ship
with a `manifest.json`.
